name: coding & stuff
markdown: kramdown
highlighter: rouge
safe: true
lsi: false
paginate: 5

exclude: ['README.md', 'Gemfile.lock', 'Gemfile', 'Rakefile']

gems:
    - jekyll-feed
    - jekyll-paginate
